// This script is only for the Timed Mode page (timed.html)

document.addEventListener('DOMContentLoaded', () => {
    // The initializeGame function is defined in script.js
    // We just call it here with the correct mode.
    initializeGame('timed');
});